services:
  movie-manager:
    image: <你的DockerHub用户名>/movie-manager:latest
    container_name: movie-manager
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - FLASK_ENV=production